# 変更履歴

[English](CHANGELOG.md) | 日本語

pocket-claude の主要な変更をここに記録します。

## [v2.11.0] - 2026-09-07

### 追加
- **API混雑による再試行が画面に見えるようになりました** — Anthropicが529 Overloadedを返すと、CLIは最大10回・指数バックオフ（約0.5秒〜約38秒、合計およそ3分）で黙って再試行します。このとき出る `system/api_retry` イベントは `text` を持たないためフロントが捨てており、**3分近く画面が完全に無反応**＝タブが死んだのと区別が付かず、停止して再送し、また混雑に当たるという体験になっていました（2026-09-03に実際に発生）。今後はオレンジ色の1行で `⏳ APIが混雑しています — 再試行中 (3/10)・約34秒後` と表示し、試行ごとに同じ行を書き換えます。APIが応答を再開すると、3回以上再試行していた場合だけ `⚠ API混雑のため3回再試行しました` の1行に畳んで痕跡を残し、1〜2回で収まった場合は跡形なく消えます。表示はライブ時のみで、履歴のレジューム再生には出ません。
- **履歴から実行中の会話を開くと、本体のセッションへ合流するようになりました** — 履歴一覧に出るIDは Claude session ID ですが、実際に走っているプロセス・ライブ配信・実行中フラグは別の pocket session ID で管理されています。従来は実行中の会話を履歴からタップすると、本体と繋がらない"影のタブ"ができてしまい、出力が伸びず・ステータスドットが緑のまま・リロードしたときだけ進捗が見える、という状態になっていました。影のタブから送信すると、同じ会話に2本目の `claude --resume` プロセスが立ってしまう副作用もありました。修正後は、実行中の会話をタップすると本体のセッションへ合流し、通常タブと同じ増分同期に乗ります——**リロード無しで本文が伸び続け**、ステータスも running を正しく示し、完了時は connected に戻ります。**既知の限界**: 合流したタブに表示されるのは「そのpocketセッションが始まって以降」の内容です。そのセッション自体が過去に履歴から復帰して始まったものだと、それ以前のやり取りは画面に出ません（意図的な仕様。生ログのリプレイとライブ配信はイベントの粒度が異なり、安全には繋げられないため）。

### セキュリティ
- **依存関係: `qs` を 6.16.0 へ強制** — `npm audit` が `qs` の moderate 3件（角括弧キーのカンマ解析による array-limit 回避、攻撃者が操作可能な `isBuffer` 経由のDoS）を検出していました。`express` → `body-parser` 経由の推移的依存で、`body-parser` が `qs` をチルダ範囲で固定しており修正版がレンジ外のため、通常の `npm audit fix` では解消できず、他の手段ではExpress 5系への移行が必要でした。`package.json` の `overrides` で修正版の `qs` を引き込み、`express`・`body-parser` のバージョンは据え置いています。脆弱性は0件に戻りました。

## [v2.10.0] - 2026-08-31

### 追加
- **ツール構成を設定パネルのallowlist方式へ刷新し、CLIから自動学習するようにしました** — 従来のハードコード23個denylist（`proxyDisallowedTools`）は既にCLIの実際のツール構成とズレていました。新設計では `toolsDirect`・`toolsProxy` の2つの独立したallowlistをブラウザの設定 → 「上級者向け: ツール構成」パネルから編集できます（プロキシ側は `proxyModels` を設定している場合のみ表示）。どちらも未設定なら何も絞りません（すべてにチェックを入れた状態は「未設定」として保存されるため、全部オンにしてある設定は将来Anthropicが追加するツールも自動で有効になります）。ツール一覧自体はセッション起動時にCLIが送る `system/init` イベントから取得して `tools-catalog.json` へキャッシュするため、Anthropicがツールを追加しても黙って漏れることなく追従します。変更は次に起動するセッションから反映されます（実行中の会話はそのまま）。`AskUserQuestion` はこの設定に関わらず常に無効のままです。効果があるのは主にプロンプトキャッシュが効かない経路（翻訳プロキシ配下の小型ローカルモデルなど）で、実測例ではツール定義24個・64,310バイトを5個・6,789バイトへ絞り、リクエスト全体は83,225→16,960バイト、ローカルOllamaでの初回応答は約40秒→約13秒、使えるコンテキストは約5万→約7万トークンになりました。Anthropicへ直接つなぐモデルはツール定義がプロンプトキャッシュに乗るため、絞っても得は小さいです。
- **履歴から復帰した会話が、そのセッションの model／effort／thinking を引き継ぐようになりました** — 従来は端末の既定値へフォールバックしていました。サーバーに残っている最終使用設定（`GET /api/session-settings/:sessionId`）を読み直して復帰タブへ適用します。

### 変更
- **リクエストボディサイズ上限の専用Saveボタンを廃止しました** — 設定パネルの `maxBodySizeMb` 編集も、新しいツール構成と同じ未保存変更フローに統合されました。ヘッダーの✕が青い「保存 ✕」に変わり、左に赤い「キャンセル」が現れます。ボディサイズ上限とツールallowlistはどちらも保存時に1回のリクエストへまとめて送られます。

### 修正
- **プロキシ経由モデルから切り替えても古いプロキシへ繋がり続ける不具合を修正** — 例えばローカルモデルから Claude のモデルへ切り替えると `set_model` 自体は成功しますが、プロセスのenv（`ANTHROPIC_BASE_URL` など）は起動時に固定されるため、CLI は新しいモデル名を報告しつつ実際の通信は古いプロキシへ流れ続けていました。切替前後で経路が異なる場合は `set_model` を試さず、`--resume` でプロセスを起動し直して正しいenvで再開します。
- **タブのモデル選択が端末の既定モデルを書き換えていた問題を修正** — 1つのタブでモデルを変えると、以後の新規タブすべての既定値が黙って上書きされていました。既定モデルの変更は設定モーダルのラジオボタンからのみ行います。
- **キャッシュ情報を返さないモデルでコンテキスト使用率が0%のままだった問題を修正** — `message_delta` の usage は「そのAPIコール1回分の全量」であるため、直前までのdeltaに積み上げるのではなく置き換えるようにしました（入力トークンを一切報告しない経路のみ従来の積算にフォールバック）。ローカルのOllamaモデルでも実際の使用率が表示されます。

## [v2.9.0] - 2026-08-21

### 追加
- **異常終了時のハンドリングを追加** — `unhandledRejection`／`uncaughtException` を捕捉するようにしました。理由とスタックトレースを必ずログへ残してから、既存の graceful shutdown 経路で終了させます（実行中セッションにはプロセスが黙って消えるのではなく正しく `done` 通知が届きます）。復旧自体はプロセスマネージャーの再起動ポリシーに任せています。
- **最小限のテストを追加** — `npm test`（`node --test`・新規依存なし）で、過去に実際の障害を起こした3箇所を押さえました。スケジューラの発火時刻衝突回避、前回サイクルで配送経路が黙って壊していたセッション設定のフォールバック（レコード → セッション保存値 → 既定）、履歴再生イベントの削減処理（描画結果が変わらないことを検証）です。
- **marked・DOMPurify をvendoring** — 両ライブラリをバージョン固定で `public/vendor/` へ取得し、そこから読み込むようにしました。従来のフロートメジャー指定・integrityなしのCDN URLをやめ、外部ネットワークへ依存せずメッセージの描画・サニタイズが動くようになりました。

### 変更
- **JSON永続化をすべて原子的な書き込みへ統一** — JSONファイルを書いている全箇所（セッション・スケジューラ・予約投稿・サーバー設定・プロジェクト）が、直接上書きではなく一時ファイルへ書いてから rename する方式になりました。書き込み中に中断（クラッシュ・OOM kill）してもファイルが半端な壊れた状態で残ることがなくなります。失敗は黙って握り潰さずログへ残し、予約投稿は保存に失敗すると画面にもその旨を表示するようになりました（従来はディスク書き込み失敗が警告ゼロのまま、次回起動で予約が消えることがありました）。
- **sessionId をすべての書き込み経路で検証するようにしました** — 履歴の読み出しに既にあったUUID形式チェックを、`POST /api/send`・`GET /api/stream`・`POST /api/register-session`・`POST /api/stop`・`POST /api/reset` にも適用しました。不正な値はファイルパスの組み立てに使われる前に400で拒否します。
- **`model` を設定済みの一覧と突き合わせるようにしました** — `config.json` に `models` を設定している場合、一覧に無い値は `--model` へそのまま渡さず400で拒否します。`models` を設定していない環境は従来どおり動作します。
- **`POST /api/client-log` に専用のボディ上限（64KB）を設けました** — 既定で無制限の `maxBodySizeMb` とは別枠です。クライアント側の不具合でサーバーのログへ無制限にデータを書き込めなくなります。

### 削除
- **`/api/tabs` エンドポイントとその背後のタブ設定サブシステムを削除しました** — タブ設定はセッション単位のファイル（`sessions/<id>.json`）へ既に移行済みで、フロントエンドから `/api/tabs` を呼ぶ箇所はもう無く、`tabs.json` は死重でした。ルート・`services/tabs.js`・未使用だった `getSessionId`／`saveSessionId` を削除しています。ディスクに `tabs.json` が残っていても単に無視されるだけなので、都合の良いときに削除してください。

## [v2.8.0] - 2026-08-21

### 追加
- **`ops/` — リポジトリに同梱する運用スクリプト** — pocket-claude を動かすホストで回している2本のcronスクリプトを、実機だけに置かずgit管理下へ移しました。`update-claude-cli.sh`（Claude Code CLI の日次更新。npmの成功だけでなく**実体**が最新に追従したかを検証し、更新時はサービスを再起動）と `claude-keepalive/keepalive.sh` です。自動配置はされません。配置先は `ops/README.md` を参照してください。
- **予約投稿にライフサイクル** — 各予約が状態（待機中／実行中／失敗）を持つようになりました。配送に失敗した予約は削除されず、理由付きで一覧に残り、再送・編集・削除ができます。削除すると本文が入力欄の草稿へ戻ります。サーバー停止中に時刻を過ぎた予約は遅れて実行されず失敗として記録され、成功メッセージは配送成功**後**に出るようになりました（従来は配送前に無条件で表示＝実態と食い違っていました）。

### 変更
- **長い会話のレジュームが大幅に速くなりました** — 所要時間はイベント件数ではなく転送バイト数に支配されていました。4.40MBの会話でも画面に描画される文字は135KBしかなく、残りはライブ配信用の封筒・重複・画面に出ないイベントです。履歴再生には専用の削減済みイベント列を送るようにし（重複する `assistant` ターン、テキストの無い `system`、thinking/signature デルタ、未使用の tool result を除去。連続するデルタは合体）、描画結果を変えずに 4.40MB/5291件 → 2.08MB/1157件、gzip後 1152KB → 640KB になりました。
- **履歴レスポンスをgzip圧縮** — events を zlib で圧縮して返します（実測 4.62MB→1.18MB、依存追加なし）。クライアントは逐次デコードします。
- **本文中のリンクを新規タブで開くようにしました** — 従来は同一タブで遷移してしまい、実行中セッションの画面が失われていました。`target="_blank"` と `rel="noopener noreferrer"` を付与します。ローカルキャッシュから復元した過去の会話のリンクにも適用されます。

### 削除
- **「未送信」プロンプト一覧を廃止** — 配送口を一本化する前の名残でした。常駐プロセスへの注入が機能している限り何も溜まらず、あっても誤解を招くだけだったためです。配送失敗は代わりに必ずシステムメッセージとして画面に出ます。

### 修正
- **履歴からのレジュームが無言で固まることがある問題** — 再生ループの `requestAnimationFrame` 内で起きた例外が握り潰され、Promiseが永久に未解決のままになっていました（％表示のまま停止し、エラーも出ず復帰もしない）。1件ごとにスキップし、致命的な例外は必ず画面へ出すようにしました。関連して、モデル切替（`set_model`）が残す `<local-command-stdout>` は `content` が文字列で来るため `TypeError` になり、再生が必ずそこで止まっていました。システム行として表示します。
- **レジュームの進捗が50%で止まって見える問題** — 分母に gzip 後の `Content-Length` を使いながら展開後のバイト数を数えていたため、全体の約25%地点で50%に張り付いていました。サーバーが返す展開後サイズを使うようにしました。
- **予約投稿が無言で配送に失敗していた問題** — v2.7.0の配送口一本化による回帰です。新設した `if (!project)` ガードが旧実装の既定プロジェクトへのフォールバックを引き継いでおらず、`claude` プロセスが生きていないタブへの予約投稿が全て失敗していました。真因はより深く、タブのプロジェクト／モデル／effort／thinking がブラウザの `localStorage` にしか無く、ブラウザ抜きで発火する経路（予約投稿・レート制限自動再開）は毎回どこかから推測する必要があったことです。これらをセッション単位でサーバー側に永続化し、「レコード → セッション保存値 → 既定」の順で解決するようにしました。自動再開もタブのモデルを引き継ぐようになり、既定モデルへ無言で落ちることがなくなりました。
- **送信に失敗すると書きかけの本文が失われることがある問題** — 送信中の本文を `pendingSend` として永続化し、配送が確定（成功応答、または `user_input` のリプレイ一致）したときだけ消すようにしました。従来の楽観クリアは応答を受け取れた場合しか復元できず、ページを閉じた・応答自体が届かない場合に消えていました。添付画像とプレビューも復元し、楽観表示した画像行は撤回します。
- **画面には出ているのにClaudeに届いていない、という食い違い** — `POST /api/send` は配送失敗時に理由付きの502を返すようになり、`user_input` のブロードキャストは子プロセスへ実際に書き込めたことを確認してから行います。注入失敗はサーバーログにも残ります。
- **レジューム後にコンテキスト％が0%へ潰れる問題** — レジューム時にCLIが吐く中身ゼロのターン（`num_turns: 0`・空の `modelUsage`）を無条件に描画していたためです。トークン情報が皆無なターンは表示も内部状態の更新も行いません。
- **再起動時に「中断」行が二重に出る問題** — graceful shutdown の予告と実プロセス終了の両方が描画されていました。`Interrupted (server restarted)` の1行にまとめます。
- **履歴から再開したタブが完全リロードで真っ白になる問題** — そのタブは自分のログを持たないためです。一度だけ穴埋めするようにしました。

## [v2.7.0] - 2026-07-26

### 追加
- **停止＝セッション終了ではなく「いまのターンの中断」に** - 停止ボタンがCLIの制御メッセージ（`control_request`/`interrupt`）をstdinへ送るようになり、走行中のターンだけが中断され常駐 `claude` プロセスは生き残ります。次のプロンプトは `--resume` 再起動なしで同一セッションのまま続きます（コンテキスト再読込・プロジェクト初期化のやり直しが起きない）。CLIがACKを返さない場合は従来の `SIGTERM` へフォールバックします。
- **モデル切替に `set_model` を使用** - アイドル中のモデル変更を、プロセスkill＋`--resume` 再起動ではなく `set_model` 制御メッセージで行います。ACKが無い場合は従来の再起動経路へフォールバックします。

### 変更
- **停止はタップした瞬間に反応** - 従来はストリームの `done` 到着を待って表示を更新していたため、切れやすいモバイル回線では「押しても止まらない」ように見えました。タップ直後に `stopping` 表示（点滅停止＋減光）へ切り替え、失敗（認証プロキシの期限切れ・オフライン・既に停止済み）は明示メッセージを出し、サーバー照合で現実に合わせます。停止時に破棄される未送信プロンプトもクライアントへ通知され、画面に残らなくなりました。
- **プロンプト配送口の一本化** - 手動送信・レート制限自動再開・予約投稿の3経路を、1つの配送関数（プロセス生存確認→注入または `--resume` 起動→失敗時は必ず画面へ通知）に統一しました。従来は自動再開と予約投稿が注入結果を見ておらず、配送失敗が無言で失われる可能性がありました。保留リストの表示も実態に合わせて「送信待ち」→「未送信」へ変更しています。

### 修正
- **新規cloneでセッションが起動できなかった** - 既定の `permissionMode` が `ask` で、現行のClaude Code CLIはこの値を拒否するため、自前の `config.json` を持たない利用者は全セッションが起動失敗していました（画面にはstderrのみ）。既定を `acceptEdits` に変更し、未知の値はサーバーログに理由を残して既定へ落とします。
- **プロンプト草稿が「無駄に残る」／「残ってほしいときに消える」** - 草稿の真実源が二重（メモリ＝入力のたび更新、localStorage＝タブ切替時のみ書き出し）だったため、逆向きに見える2症状が同じ原因で発生していました。送信済みの文がリロードで入力欄に復活する一方、打ちかけの文はページが飛ぶ（認証プロキシの期限切れなど）と失われていました。書き込み口を `setDraft()` 1本にして両方を必ず同時に更新します（入力中はデバウンス、送信クリア・送信失敗の復元・タブ切替は即時）。なお送信失敗時に**添付画像は復元されません**（テキストのみ）。
- **送信失敗の復元が別タブの入力欄を汚す** - 復元が共有の入力欄へ直接書き込んでいたため、リクエスト中にタブを切り替えると前タブの文が新しいタブに刺さっていました。復元対象をセッション単位にし、そのセッションが前面のときだけ入力欄へ反映します。
- **「認証の期限が切れました」を名指しで表示** - 認証リバースプロキシ（Authelia等）の期限切れ時、`POST /api/send` は別オリジンへの302を受け、`follow` のfetchではCORSのTypeErrorになるため画面には「Connection failed」としか出ませんでした。`redirect: 'manual'` にして「認証の期限が切れました。ページを再読み込みしてログインし直してください」と表示します。
- **停止していないターンが「停止しました」と表示される** - resultのsubtypeだけで「ユーザーが停止した」と判定していたため、本物の実行時エラーまで「停止しました」に化けて失敗を隠していました。サーバーが `interrupted` マーカーを出し直後の `result` が順序で消費する方式に変更し、アイドル時（interruptが安全なno-opになる場合）はマーカーを出しません（従来は残ったマーカーが**次の**正常完了ターンを誤って「停止しました」と表示していました）。

## [v2.6.0] - 2026-07-22

### ドキュメント
- **モデル選択ガイド** - モデルのプルダウンが CLI のティアエイリアス（`fable`/`opus`/`sonnet`/`haiku`）を使い各ティアの最新モデルへ自動解決すること、Claude Code CLI を最新に保つ重要性（CLI が古いとエイリアスが古いモデルへ解決される）、特定・古いモデルへピン留めする方法（正確なモデルIDを選択肢の `value` に設定）を README に記載しました。
- **設定・使い方メモ** - 設定パネル（Effort・テーマ・フォントサイズ・言語・リクエストボディサイズ上限）と使い方メモ（タップ選択ではなくテキストで来る質問・画像添付・予約投稿・レート制限自動再開）を記載しました。

### 備考
- v2.5.0 タグの後に載ったモデルエイリアスのプルダウン再設計コミットを含みます。

## [v2.5.0] - 2026-07-22

### 追加
- **モデルをCLIエイリアスで指定し、プルダウンに実解決モデルを表示** - モデルをピン留めIDではなく Claude CLI のエイリアス（`fable`/`opus`/`sonnet`/`haiku`）で選ぶようにしました。各ティアが常に現行の最新モデルへ自動解決され、バージョンIDを手で維持する必要がなくなります。プルダウンは各項目のラベルに、CLIが実際に解決した具体モデル名（`system/init` イベントの `model` フィールドから実測）を反映します。例えば **Default** は `Default (Sonnet 5)` と表示され、CLI既定が何で走るか一目で分かります。モデル名はハードコードせず、CLIの解決結果にラベルが追従します。従来のヘッダー横の別表示（矢印キャプション）は廃止し、プルダウン内表示に統一しました。

### 変更
- **Fable 5 のモデルラベルに Pro 従量課金を明示** - 2026-07-20 より Anthropic は Fable 5 を Max / Team Premium ではサブスクに含める一方、Pro / Team Standard では一度きりの $100 クレジット消化後に従量課金（トークン単価）に切り替えます。pocket-claude は利用者のプランを判別できないため、既定のモデルリストで `Fable 5 (Pro: metered)` と表示し、Pro ユーザーが不意の課金に気付けるようにしました（Max ユーザーは無視可）。詳細とラベル変更方法は README を参照。

### セキュリティ
- **body-parser を 1.20.6 へ更新** - 不正な `limit` 値でリクエストボディのサイズ制限が黙って無効化される低深刻度のDoS脆弱性（GHSA-v422-hmwv-36x6）を解消。express 経由の間接依存で、lockfile のみの変更・API への影響なし。

## [v2.4.0] - 2026-07-18

### 追加
- **バインド先アドレスの設定** - `config.json` の `host`（または環境変数 `HOST`）で listen するインターフェースを限定できるように。既定は従来どおり `0.0.0.0`。リバースプロキシ経由のアクセスのみに絞りたい場合に特定インターフェースのIPを指定する。

### 修正
- **再開カードが会話の途中に埋まる問題** - レート制限の再開カードを、会話フローへの追記から入力欄直上の専用スロット表示に変更。バックグラウンド復帰・再認証後に履歴再生とカード復元が並走しても、カードが会話の途中に埋まらなくなった。トレードオフとして、カードは制限発生位置のインラインではなく常に入力欄の上に表示される。
- **アプリ復帰後に「実行中」表示が残る問題** - バックグラウンド中にターンが完了した場合、復帰後もステータスドットの実行中点滅とモデル選択ロックが残ることがあった。再接続時の照合をサーバーとの双方向照合に統一して解消。あわせて、実行中の再接続のたびにドットの形が一瞬跳ねる見た目の問題も解消。

### ドキュメント
- README: `proxyModels` を文書化 — 特定モデルだけを任意のAnthropic互換プロキシ経由にできる仕組み。v2.3.0の「GLM-5.2（Cloudflare）」はこの仕組みの利用例であり、内蔵モデルではない。
- スクリーンショットを更新（汎用パス表記に）。

## [v2.3.0] - 2026-07-01

### 追加
- **GLM-5.2（Cloudflare）モデルの追加** - opt-in方式の代替バックエンド。明示的に選択したセッションだけが翻訳プロキシ経由になり、既定のClaude経路はプロキシの生死から完全に独立。
- **自動再開の改善** - レート制限の自動再開はリセット時刻+3分（従来+60秒）まで待機してから再送するように拡大し、複数セッションが同時に制限解除待ちになった場合は3分ずつずらして再開（一斉再送による再制限を回避）。再開カードには実際の再開予定時刻を表示。
- **自動再開デフォルトON設定** - 制限のたびに手動でONにしなくても自動再開する設定を追加。サーバー側で保持されるため、制限発生時にクライアントが切断中でも再開登録される。制限中に予約投稿モーダルを開くと日時欄が最初からリセット時刻になる。

### 修正
- **リロード時の会話の瞬時復元** - 描画済みの会話HTMLをキャッシュ（IndexedDB）し、ページリロード時に即座に復元→以降は増分のみ同期。従来の全履歴再取得・再描画をやめた。
- **背面タブの状態消失** - 再開カード・実行中バッジ・送信待ちキューを、前面タブだけでなく開いている全タブについてリロード・再接続・アプリ復帰時にサーバーと照合するようにし、背面タブに残っていた状態が消える問題を解消。
- **再接続時のリプレイによる状態の残留・消失** - 再接続後のログ再生をライブイベントと区別してタグ付けし、過去ログの再生が解決済みのレート制限カードを復活させたり、まだ有効なカードを誤って消したりしないようにした。
- **完全リロード時の会話真っ黒バグ** - ハードリロードでキャッシュ済みの会話が表示されず空になる不具合を修正。
- **再開カードの二重表示とモデル切替の遅延** - 再開カードが稀に二重表示される問題と、モデル切替が次のメッセージまで反映されない問題を修正。

### 変更
- モデルリストを更新：Claude Sonnet 4.6 → Claude Sonnet 5（`claude-sonnet-5`）に差し替え。

## [v2.2.0] - 2026-06-12

### セキュリティ
- **死にコード `config/security.js` を削除** - `isPathAllowed()` / `ALLOWED_BASE_DIRS` を定義していたがプロジェクト読み込みに配線されておらず、「防御しているように見えて防御していない」状態だった。プロジェクトディレクトリの信頼モデル（ネットワーク/認証層へ委譲）を README に明記する方針に統一。
- **`git pull` を堅牢化** - `--no-verify`（`post-merge` 等のリポジトリ hook 発火を防止）と `GIT_TERMINAL_PROMPT=0`（認証待ちハング防止）を付与。
- **プロセスエラーで内部情報を漏らさない** - spawn エラーはサーバーログに記録し、クライアントには一般メッセージのみ返す。
- **依存パッケージ更新** - `npm audit fix` で既知脆弱性 4 件（path-to-regexp, qs, body-parser, express）を解消（残 0 件）。

### 変更（内部）
- `getClaudeSessionId` / `saveClaudeSessionId` を `services/sessions.js` に集約。`services/spawner.js` の重複実装を撤去（DRY）。
- SSE のメモリ内バッファをセッションあたり 5000 件で上限化。長時間セッションでのメモリ単調増加を防止（完全な履歴はログファイルに永続化される）。

### ドキュメント
- README: プロジェクトディレクトリの信頼モデルを明記。陳腐化していた「systemd 連携」の記述を「ポート使用中ガード」に修正。

## [v2.1.0] - 2026-06-11

### 廃止
- **AskUserQuestion 選択肢 UI** - ツールを CLI レベルで無効化（`--disallowed-tools AskUserQuestion`）。ヘッドレス stream-json モードでは、回答を注入する前に CLI 自身がツール呼び出しをエラーで自己解決するため、タップ式の選択肢 UI は原理的に成立しない。今後モデルは質問を素のテキストで行い、通常の入力欄で回答する。`/api/respond` エンドポイントと関連 UI コードは撤去

### 追加
- **Claude Fable 5** を内蔵モデルフォールバックリストに追加（Opus 4.7 → 4.8 も更新）
- **最下部スクロールボタン** - 出力エリアを上にスクロールすると円形ボタンを表示
- **再開時の進捗表示** - セッション再開時に空白画面ではなくパーセント進捗を表示
- 画像添付の UX 改善

### 修正
- レート制限パネルと送信待ちキューパネルが SSE 再接続時にサーバーと再同期するように修正；バックグラウンド中に自動再開・キュー消化が起きてもパネルが残留しない
- 自動再開の ON 登録が後続のレート制限イベントで破棄される問題を修正
- 自動再開 ON ボタンのサイレント失敗を修正；スケジュール登録をログに記録
- `config.json` に追加した新モデルがセレクターに自動表示されるように修正
- バックグラウンド復帰後の SSE 凍結を修正；実行中セッションは画面復帰時に常に再接続
- バックグラウンド復帰後のログ重複描画を修正
- レート制限カードがリロード・タブ切り替え・リセット時刻超過後に再表示される問題を修正
- API レスポンスの Cloudflare キャッシュを回避する `Surrogate-Control: no-store` を追加
- 設定モーダルの入力欄で iOS Safari が自動ズームする問題を修正

### 内部変更
- `projects.json` と `schedules.json` をバージョン管理から除外（ローカル状態ファイル；`projects.example.json` を参照）

## [v2.0.0] - 2026-05-09

### 追加
- **リクエストボディサイズ制限** - 設定画面から変更可能（MB 単位；0 = 無制限）；サーバー再起動なしで即時反映；`config.json` に保存
- **モデル一覧 API** (`GET /api/models`) - `config.json` からモデルリストを返す；取得失敗時は内蔵フォールバックリストを使用
- **画像添付** - プロンプトに画像を添付（base64、複数枚対応）
- **ステータスアイコンへの停止機能統合** - ヘッダーのステータスアイコンに停止ボタンを統合
- **クリップボードボタン** - ヘッダーにクイックコピーボタンを追加

### 修正
- `turning` フラグ導入によりアイドル状態の誤検知を抑制
- SSE 再接続時に `isRunning=true` が誤送出される問題を修正
- 予約投稿カードのプロンプト表示が途中で切れる問題を修正
- SSE 再接続後に実行中ステータスが正しく反映されない問題を修正
- ステータスアイコンのサイズ・アニメーション・見た目を修正
- ブラウザリロード後の履歴再生で ctx% が表示されない問題を修正
- スケジュールモーダルの入力欄で iOS Safari が自動ズームする問題を修正
- 常駐プロセスモードで自動再開・予約投稿・ステータスアイコンが機能しない問題を修正

### 内部変更
- `getClaudeSessionId()` を 3 ファイルの重複実装から `services/sessions.js` に集約

## [2026-03-28]

### 追加
- **予約投稿** - 指定日時にプロンプトを自動送信；出力パネルから編集・キャンセル可能；`scheduled-posts.json` に永続化（サーバー再起動後も復元）
- **言語切り替え** - 設定画面から日本語 / 英語 を切り替え可能；localStorage に永続化
- **レート制限自動再開** - レート制限発生時にパネルを表示してサーバーサイド自動再開を有効化；スケジュールを `schedules.json` に永続化
- **AskUserQuestion 表示改善** - ツール結果を自動展開して質問・選択肢をリスト表示
- **コードフォントサイズ設定** - 出力フォントサイズとは独立して設定可能
- 新規タブ作成時に現在のタブのモデル・Effort・Thinking 設定を継承
- Thinking コントロールをチェックボックスから 3 ステートセグメント（指定なし / オン / オフ）に変更

### 変更
- ツール展開表示を VS Code 拡張スタイルに刷新：Bash は IN/OUT 形式、Edit は赤/緑の diff 形式
- レート制限メッセージを赤字で表示；リセット時刻を目立つ表示に変更
- stderr の使用制限メッセージを `[err]` プレフィックスなしで `line-error`（赤）で直接表示
- モデルセレクタを左揃えに変更；Send ボタンの高さが textarea に連動
- ヘッダーボタンの高さを 30px に統一
- 設定 UI の Effort・Thinking ラベルを日本語化

### 修正
- CLI コマンドのパースエラー（`is_error: true`）が無視されていた問題を修正；赤字で表示
- HTTP 環境向け `crypto.randomUUID` ポリフィルを追加
- `parseResetTime` が `am/pm + IANA タイムゾーン` 形式に対応
- 予約リスト表示と履歴読み込みの競合（レースコンディション）を修正
- プロジェクト切り替え後、設定を閉じずに選択状態を即時更新するよう修正

## [2026-03-21]

### 追加
- **履歴ブラウザ** - 過去のセッションを閲覧・再開
- **SSE 出力バッファリング** - 再接続時に出力を復元
- **複数テーマ** - Blue Dark / Purple Dark（CSS 変数 + themes.js で管理）
- **コンテキスト使用量バー** - リアルタイムでトークン % を表示
- **プロジェクト管理** - ブラウザ UI からプロジェクトディレクトリを追加・削除；`ADDITIONAL_ALLOWED_DIRS` 環境変数サポート
- **グレースフルシャットダウン** - SIGTERM 対応
- **起動時自動修復** - 前回クラッシュ時の未完了ログエントリを自動でクローズ

### 変更
- タブ管理を localStorage のみに変更（サーバー側の tabs.json 廃止）
- セッション永続化を分離：pocket-session ID と Claude session ID を独立管理
